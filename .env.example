# URL publique de l'API de synchronisation (backend server/).
# Laisser vide pour un fonctionnement 100% local (sans synchro).
# En production, pointer vers votre domaine HTTPS (obligatoire pour iOS).
VITE_API_URL=https://api.votre-domaine.fr
